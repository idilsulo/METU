python the1.py feature_des k

feature_des: Takes two different values SIFT and DENSE to select the feature descriptor algorithm to be used.

k: Takes four different values to select the k value for the k-means algorithm where k is one of the following values from the set {32, 64, 128, 256}
